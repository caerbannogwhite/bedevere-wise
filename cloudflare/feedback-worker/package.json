{
  "name": "bedevere-feedback-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "schema:apply": "wrangler d1 execute bedevere-feedback --file=./schema.sql --remote",
    "schema:apply-local": "wrangler d1 execute bedevere-feedback --file=./schema.sql --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250923.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.78.0"
  }
}
